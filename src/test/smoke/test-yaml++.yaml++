---
"$extends":
  - base/normal.json
description:
  - This is a passing example NORMAL test
when:
  description:
    - Just print a message 'Hello world'
  source:
    - ${BUD_LIB}/common.rc
  cmd: echo
  args:
    - "-e"
    - hello, world
then:
  stdout:
    present:
      - regex:hello.+world
  stderr:
    absent:
      - regex:.+
