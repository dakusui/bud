---
"$extends":
  - base/normal.json
description:
  - Call `abort` function and make sure it works as designed.
when:
  description:
    - Call 'app_name' function.
  source:
    - ${BUD_LIB}/core.rc
  cmd: app_name
then:
  exitCode:
    - EQUAL
    - 0
  stdout:
    present:
      - regex:bud
  stderr:
    absent:
      - regex:.+
